# Generated by hj github-ci. Do not edit.
# hj-workflow-cli: github:hugojosefson/cli@f43b1c8419ab3400e3cc79216a9f6c7571804f1b
# hj-ci-runtime-matrix: deno-node-bun-v1
name: CI

on:
  pull_request:

permissions:
  contents: read

cache-mode: read

concurrency:
  group: hj-ci-${{ github.workflow }}-pr-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  deno:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: denoland/setup-deno@22d081ff2d3a40755e97629de92e3bcbfa7cf2ed # v2.0.5
        with:
          deno-version: 2.9.6
          cache: true
          cache-hash: deno-2.9.6-${{ hashFiles('**/deno.lock', 'toolchain.json') }}
      - run: deno task ci-deno
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: runtime-report-deno
          path: .hj/test-results/deno.json
          include-hidden-files: true
          if-no-files-found: error
          retention-days: 1
          overwrite: true
  native:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        runtime: [node24, node26, bun]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: denoland/setup-deno@22d081ff2d3a40755e97629de92e3bcbfa7cf2ed # v2.0.5
        with:
          deno-version: 2.9.6
          cache: true
          cache-hash: deno-2.9.6-${{ hashFiles('**/deno.lock', 'toolchain.json') }}
      - name: Cache native test npm downloads
        if: hashFiles('scripts/test-build/dependencies/package-lock.json') != '' && hashFiles('scripts/test-build/tools/package-lock.json') != ''
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.npm
          key: native-test-npm-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('scripts/test-build/dependencies/package-lock.json', 'scripts/test-build/tools/package-lock.json', 'toolchain.json') }}
          restore-keys: |
            native-test-npm-${{ runner.os }}-${{ runner.arch }}-
      - run: deno run --allow-all scripts/run-test-matrix.ts --runtime ${{ matrix.runtime }}
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: runtime-report-${{ matrix.runtime }}
          path: .hj/test-results/${{ matrix.runtime }}.json
          include-hidden-files: true
          if-no-files-found: error
          retention-days: 1
          overwrite: true
  check:
    if: ${{ always() }}
    needs: [deno, native]
    runs-on: ubuntu-latest
    steps:
      - name: Require successful checks and every runtime
        env:
          DENO_RESULT: ${{ needs.deno.result }}
          NATIVE_RESULT: ${{ needs.native.result }}
        run: test "$DENO_RESULT" = success && test "$NATIVE_RESULT" = success
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: denoland/setup-deno@22d081ff2d3a40755e97629de92e3bcbfa7cf2ed # v2.0.5
        with:
          deno-version: 2.9.6
          cache: true
          cache-hash: deno-2.9.6-${{ hashFiles('**/deno.lock', 'toolchain.json') }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: runtime-report-*
          path: .hj/test-results
          merge-multiple: true
      - name: Compare complete executed inventories
        run: deno run --allow-read scripts/run-test-matrix.ts --compare-only
  hj-release-commit-validation:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: denoland/setup-deno@22d081ff2d3a40755e97629de92e3bcbfa7cf2ed # v2.0.5
        with:
          deno-version: 2.9.6
          cache: true
          cache-hash: deno-2.9.6-${{ hashFiles('**/deno.lock', 'toolchain.json') }}
      - name: Validate release commits
        env:
          HJ_RELEASE_ROUTE: source-validation
          HJ_SOURCE_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HJ_SOURCE_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: >-
          deno run --no-lock
          --allow-env=HJ_RELEASE_ROUTE,HJ_SOURCE_BASE_SHA,HJ_SOURCE_HEAD_SHA
          --allow-run=git
          --import-map=https://raw.githubusercontent.com/hugojosefson/cli/f43b1c8419ab3400e3cc79216a9f6c7571804f1b/deno.json https://raw.githubusercontent.com/hugojosefson/cli/f43b1c8419ab3400e3cc79216a9f6c7571804f1b/src/cli/cli.ts
          release publish-tag-prepare
