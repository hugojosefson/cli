# hugojosefson-cli (`hj`)

Similar to `gh` in some ways, and for some purposes, but with entirely my own
opinionated configs and features.

## Usage

```bash
hj repo init       # initializes local git repo, with templated features
```
